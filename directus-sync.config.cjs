module.exports = {
  // !NOTE: pass auth-data via CLI!
  // debug: true,
  // The instance to pull from / push to. By default use our local instance, can be changed to staging or production on update.
  // directusUrl: 'http://127.0.0.1:8055',
  dumpPath: './directus-config',
};